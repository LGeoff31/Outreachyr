import type { ReactNode } from "react";

export const metadata = {
  title: "Sign in - Outreachyr",
};

export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
