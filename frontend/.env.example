NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_CONTACT_EMAIL=

# Optional — sends feedback to geoffrey31415@gmail.com via Resend. If unset, FormSubmit is used.
RESEND_API_KEY=
RESEND_FROM=Outreachyr <onboarding@resend.dev>

# Optional. Local dev uses http://127.0.0.1:5050. On Vercel prod, set to
# https://outreachyr.com/_/backend (or leave unset — auth callback infers it).
OUTREACH_API_URL=
