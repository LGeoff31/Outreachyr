{
  "name": "outreachyr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "docker compose up --build --watch",
    "dev:backend": "docker compose up --build --watch backend",
    "dev:frontend": "docker compose up --build --watch --no-deps frontend"
  }
}
